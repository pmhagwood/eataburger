<li>
	{{name}}

	{{!-- <button class="change-sleep" data-id="{{id}}" data-newsleep="{{eaten}}">
		{{#if sleep}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button> --}}

	<button class="devour-burger" data-id="{{this.id}}">{{#if eaten}}DEVOUR IT!{{else}}{{/if}}</button>
</li>
