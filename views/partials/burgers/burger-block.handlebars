<li>
	{{name}}

	<button class="devour-burger" data-id="{{id}}" data-eatburger="{{eaten}}">
		{{#if eaten}}DEVOUR IT!{{/if}}
	</button>

	{{!-- <button class="devour-burger" data-id="{{this.id}}">{{#if eaten}}DEVOUR IT!{{else}}{{/if}}</button> --}}
</li>
